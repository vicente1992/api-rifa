const { validateCreateTickect } = require('./validateCreateTickect')
const { validateGetTickect } = require('./validateGetTickect')
const { validateUpdateTickect } = require('./validateUpdateTickect')
module.exports = {
  validateCreateTickect,
  validateUpdateTickect,
  validateGetTickect
}

